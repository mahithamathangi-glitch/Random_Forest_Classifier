# Git Rollback Evidence

## Stable Version

The completed Random Forest Classifier project was tagged as:

`v1.0`

The `v1.0` tag represents the stable version of the project.

---

## Rollback Demonstration

A separate branch named `rollback-demo` was created to demonstrate the rollback workflow.

The following temporary change was made to the README:

```text
## Rollback Test